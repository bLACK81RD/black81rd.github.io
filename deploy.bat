@echo off
:: ============================================================
::  🚀  Что делать после правок сайта (порядок действий)
::  1️⃣  В VS Code сохранить изменения (Stage All Changes → Commit)
::  2️⃣  Отправить их в main (можно через VS Code или автоматически)
::  3️⃣  Запустить этот файл (deploy.bat) для публикации сайта
::      Он сам:
::        • добавит и закоммитит изменения с текущей датой
::        • отправит их в ветку main
::        • обновит сайт на GitHub Pages (ветка gh-pages)
:: ============================================================

cd /d "D:\_GitHub\black81rd.github.io"

:: Получаем текущие дату и время для коммита
for /f "tokens=1-5 delims=/:. " %%a in ("%date% %time%") do (
    set DATETIME=%%a-%%b-%%c_%%d-%%e
)

:: Добавляем, коммитим и пушим изменения
git add .
git commit -m "Site update: %DATETIME%"
git push origin main

:: Обновляем сайт на GitHub Pages (заливает только папку site)
git subtree push --prefix site origin gh-pages

echo ============================================================
echo ✅ Сайт успешно обновлен!
echo 🌐 Проверь: https://black81rd.github.io/
echo ============================================================
pause
